# Copy this file to .env for local development
# For production on Render, these will be set automatically

# Database URL for PostgreSQL (required)
# For local development, defaults to: postgresql://localhost/huckleberry
# DATABASE_URL=postgresql://username:password@localhost:5432/huckleberry

# Secret key for Flask sessions (optional, defaults to 'dev' for local)
# SECRET_KEY=your-secret-key-here